<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.umesh.sphnix">

    <application
        android:name=".SphnixApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plantix_App"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".dashboard.persentation.ui.DashBoardActivity"
            android:exported="true" />
        <activity
            android:name=".auth.persentation.ui.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".auth.persentation.ui.SignupActivity"
            android:exported="true" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dashboard.persentation.ui.adapter.MainActivity"
            android:exported="true" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>